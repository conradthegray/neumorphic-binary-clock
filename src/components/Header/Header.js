import React from 'react';
import './Header.css';

const Header = () => (
  <header className="App-header">
    <span>Neumorphic</span>
    <h1>Binary Clock</h1>
  </header>
);

export default Header;
